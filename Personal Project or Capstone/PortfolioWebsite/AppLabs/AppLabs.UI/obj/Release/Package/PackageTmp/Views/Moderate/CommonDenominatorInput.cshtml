@model AppLabs.UI.Models.CommonDenominatorModel

@{
    ViewBag.Title = "CommonDenominatorInput";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Find Greatest Common Denominator</h2>

<div class="row">
    <div class="col-sm-12">
        <p>Enter two fractions and find the greatest common denominator</p>
    </div>
</div>
<div class="row">
    <div class="col-sm-4 col-sm-offset-1">
        @using (Html.BeginForm("CommonDenominatorInput", "Moderate", FormMethod.Post, new { role = "form" }))
        {
            @Html.ValidationSummary()
            <p>1 / </p> <div class="form-group" id="gcdInput">
        @Html.EditorFor(m => m.UserDenominator1, new { @class = "form-control",  placeholder = "Enter the denominator" })
    </div>
    <p>1 / </p><div class="form-group" id="gcdInput">
         @Html.EditorFor(m => m.UserDenominator2, new { @class = "form-control",  placeholder = "Enter the denominator" })
    </div>

            <button type="submit" class="btn btn-default btn-lg btn-block">Find</button>
        }
    </div>
</div>